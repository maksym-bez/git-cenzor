import { SortFirstPipe } from './sort-first.pipe';

describe('SortFirstPipe', () => {
  it('create an instance', () => {
    const pipe = new SortFirstPipe();
    expect(pipe).toBeTruthy();
  });
});
